<?xml version="1.0" encoding="utf-8"?>
<!--
FIX: no network security config meant cleartext HTTP was blocked by default
(Android 9+), so every call to http://10.0.2.2:8000 failed with
"CLEARTEXT communication to 10.0.2.2 not permitted". Allow cleartext only
for local dev hosts - never a blanket usesCleartextTraffic="true".

NOTE: Android's <domain> element does not support CIDR ranges, so a physical
device on your LAN needs its dev machine's exact IP added below (see
LOCAL_SETUP.md troubleshooting) - "192.168.*" cannot be expressed here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
